*.exe
*.exe~
*.dll
*.so
*.dylib
*.wasm

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work
go.work.sum

# env file
.env

bin/
otelcol-dev/
ocb

# We're going to ignore this for now to let users to have their own CLAUDE.md.
# We could decide later whether we should commit our own CLAUDE.md managed by the community.
CLAUDE.md
